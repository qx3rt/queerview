import { defineConfig } from 'astro/config';

// Update `site` to your GitHub Pages URL (https://<user>.github.io) and
// `base` to your repo name (e.g. '/queerview') before deploying, unless
// this repo is served from a user/org root page.
export default defineConfig({
  site: 'https://sharon-malfesi.github.io',
  base: '/queerview',
});
